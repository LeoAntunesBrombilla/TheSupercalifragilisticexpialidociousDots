require("nd-modules.nd-settings.init")
require("nd-modules.nd-terminal.init")
require("nd-modules.nd-mappings.init")
require("nd-modules.nd-misc.init")

-- load this only if your colorscheme isn't loading it by default or load it after your colorscheme is set
-- require('nd-modules.nd-higroups.init')
